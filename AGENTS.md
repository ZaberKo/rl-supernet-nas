# AGENTS.md

## Project Environment

This project uses the local `.venv` virtual environment.

Before running tests or ad-hoc Python scripts, activate the environment first:

```bash
source .venv/bin/activate
python xxx.py
```

Replace `xxx.py` with the target script for the task.

Do not run project Python commands without activating `.venv` unless the user explicitly requests it.
